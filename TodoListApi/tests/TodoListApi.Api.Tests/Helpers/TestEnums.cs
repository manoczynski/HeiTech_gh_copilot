using System;

namespace TodoListApi.Domain.Entities
{
    // Only add this if the Priority enum doesn't exist in referenced projects
    public enum Priority
    {
        Low = 0,
        Medium = 1,
        High = 2
    }
}
